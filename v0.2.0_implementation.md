# v0.2.0 Implementation Report

Branch: `v0.2.0` (off `main`). Status: implementation complete; final daemon-test triage + the G2 alignment gate in progress. Not pushed; no PRs opened.

This documents **what** shipped and **how** it was built.

---

## 1. What shipped

| # | Change | Result |
|---|--------|--------|
| 1 | Remove **image / video / audio** media generation | Removed end-to-end (chips, daemon engine, BYOK tools, CLI, prompts, contracts type-purge, tests) |
| 2 | Remove **HyperFrames** | Full purge (chip, model, 40+ templates/skills/plugins/prompt-templates, marketplace entries, analytics) |
| 3 | Remove **Live Artifacts** | Full excision (chip, ~3.2k-line runtime, viewer UI, MCP tools, routes, CLI, contracts, i18n, tests) — **connectors preserved** via an MCP server split |
| 4 | Remove **Settings › Media providers** | UI + dead write path removed; **`resolveProviderConfig` kept** (memory/research/X-search still resolve creds) |
| 5 | Add **Report** chip | New `report` chip → new general-purpose `example-report` flowing-document scenario seed; `intent:'report'` swapped in where `live-artifact` was |
| 6 | Fix **deck arrow double-advance** | `defaultPrevented` dedupe guard in all 3 synced deck-framework copies + prompt directive; JSDOM red spec |
| 7 | Fix **PDF prints one slide** | Injected print CSS force-reveals every slide in both `exports.ts` + desktop `pdf-export.ts`; behavioral red spec + copy-identity guard |
| 8 | **Remove Orbit entirely** *(added mid-implementation — see §4)* | The Live-Artifacts removal broke Orbit (its runs used `skillId:'live-artifact'` + a "create a Live Artifact" prompt). You chose full removal — excised from daemon/web/contracts/desktop/e2e. General Automations/Routines kept. |

---

## 2. How it was built — orchestration

Per your directive, the work was driven by **three Opus "middle-manager" subagents** (one per task-group), each **orchestrating only** and delegating the actual edits/review to **Codex `gpt-5.5` `xhigh`** agents over the Codex `app-server` JSON-RPC protocol, with an **Opus alignment reviewer** gating each group's "DONE".

- **Harness built + smoke-tested:** a write-mode Codex driver (`codex_agent.mjs`) — spawns a fresh `codex app-server`, runs one autonomous `gpt-5.5`/`xhigh` turn in `danger-full-access` sandbox, auto-approves exec/patch requests; plus a read-only review mode returning a strict JSON verdict. Protocol/params derived from the generated TS bindings.
- **Group split & concurrency:** **G3 (deck fixes)** and **G1 (media teardown, XL)** ran **in parallel** — G3 on the main tree, G1 in an isolated `git worktree` (disjoint file sets) — then G1 was merged into `v0.2.0` conflict-free. **G2 (Live Artifacts + Report)** ran on the merged tree (it shares `chips.ts`/contracts with G1, so it was sequenced after).
- **Restart resilience:** the environment restarted the process mid-run twice; managers were switched to **commit after every verified sub-task**, so progress survived.
- **Codex reviewer drill:** fresh Codex `app-server` instances were used to review diffs (structured JSON verdict), separate from the implementing agents.
- **Pivot to parallel Sonnets:** G2's Opus manager repeatedly stalled at coordination points (the environment auto-backgrounds long Codex turns, so the manager kept yielding). Per your instruction ("run multiple sonnets, not a lazy Opus"), the G2 finish was taken over directly with **fleets of parallel Sonnet workers editing files directly** (dropping the Codex indirection for speed/reliability): e.g. a 6-way file-disjoint fan-out for the live-artifacts removal, a dedicated worker for the 1,166-line surgical removal from `FileViewer.tsx` (precise edits only, to avoid an encoding-corruption that a full rewrite had caused), and a 4-way fan-out for the Orbit excision.
- **Verification-driven:** `pnpm typecheck` was the completeness driver (esp. the media type-purge and the Orbit cascade); a **typecheck-batch loop** (run typecheck → feed bounded error batches to workers) reconciled cross-cutting removals.

Alignment gates: **G3 → "perfect alignment, without caveats"** ✓ · **G1 → "perfect alignment, without caveats"** ✓ · **G2 → pending final gate**.

---

## 3. What was done — by workstream

- **WS-A — deck arrow double-advance:** added `if (e.defaultPrevented) return;` as the first line of `onKey` in all 3 byte-synced deck-framework copies (`apps/daemon/src/prompts/deck-framework.ts`, `packages/contracts/src/prompts/deck-framework.ts`, `templates/deck-framework.html`) + updated the prompt directive. Red spec (`design-template-deck-nav.test.ts`) RED(idx=2)→GREEN(idx=1).
- **WS-B — PDF one-slide:** hardened `DECK_PRINT_CSS` to force-reveal every slide (`display:flex/visibility:visible/opacity:1/inset:auto !important` + broadened `:not(.active)` selectors) in both `apps/web/src/runtime/exports.ts` and `apps/desktop/src/main/pdf-export.ts`; behavioral cascade-resolver red spec + a byte-identity guard between the two copies.
- **WS-C — media teardown (XL):** deleted the daemon media engine (`media.ts`/models/tasks/policy/adapters/elevenlabs), BYOK `generate_image/video/speech`, `od media` CLI, media prompt-contract + system/discovery media blocks (+ contracts mirrors), the full contracts type-purge (`ProjectKind`/`ProjectMetadata`/`api/media.ts`/`TrackingProjectKind`/`scenario-defaults`), HyperFrames content (40+ files) + 56 marketplace entries, and the media Home-Hero chips. `pnpm typecheck` drove completeness. **Preserved:** `resolveProviderConfig`/`ENV_KEYS`/`readMaskedConfig`, `amr-image-staging.ts`, `slide_deck`, and the **generic** image/video/audio file-type/export analytics enums (a C7 over-reach was caught and reverted).
- **WS-D — media settings:** removed `MediaProvidersSection` + BYOK rows + nav + `XaiOAuthControl`, the boot/save sync in `App.tsx`/`state/config.ts`, `PUT /api/media/config` + `writeConfig`; kept `media-routes.ts` (multi-purpose) pruned to non-media routes; **removed `GET /api/media/config`** (orphaned).
- **WS-E — Live Artifacts excision:** **split** `mcp-live-artifacts-server.ts` → `mcp-connectors-server.ts` (connectors-only), renamed `buildLiveArtifactsMcpServersForAgent`→`buildConnectorsMcpServersForAgent`, relocated shared `bounded-json` infra to `connectors/`; deleted the daemon `live-artifacts/` runtime, CLI, routes; removed the `LiveArtifactBadges`/inline viewer/refresh UI + SSE + providers + types + the **1,166-line** live-artifact feature inside `FileViewer.tsx` (surgical, precise-edit only); deleted `api/live-artifacts.ts` + the `example-live-artifact` plugin + intent; ~90 i18n keys.
- **WS-F — Report chip:** added the `report` chip (icon `file`), widened `ProjectMetadata.intent` to `'report'`, added the `intent==='report' → example-report` scenario-default; authored a new general-purpose `plugins/_official/examples/example-report/` flowing-document seed + marketplace entry + i18n.
- **WS-G — Orbit removal (added):** deleted `orbit.ts`/`orbit-agent-summary.ts`, the Orbit run block + template resolver in `server.ts`, `/api/orbit/*` routes, `api/orbit.ts` contracts + app-config/analytics fields, the ~829-line `OrbitSection` + ~1,300 lines of Orbit CSS + 81 i18n keys in web, the Orbit automation template (general automations kept), desktop refs, and the e2e Orbit specs; removed the now-dead `live-artifact` + `media-image/video/audio` atoms.

---

## 4. Decisions made on your behalf (all revealed, per your instruction)

From your locked answers + the resolutions the managers/alignment-reviewers made:

1. **Media generation removed entirely**, so WS-D collapsed to keeping `resolveProviderConfig` only for memory-llm(openai)/research(tavily)/X-search(grok).
2. **`GET /api/media/config` removed** (traced: no consumer survived) — post-removal config is env vars / `media-config.json`. `media-routes.ts` kept (it also serves research/app-config/dialog), pruned to non-media.
3. **`SettingsSection 'media'` dropped** across all 4 sites.
4. **`amr-image-staging.ts` kept** (verified: attachment-input staging, not generation).
5. **Codex-imagegen-override removed** end-to-end — its dead import was crashing daemon boot (found by running tests).
6. **`aihubmix.ts` kept** (generic provider client used by chat/memory/research).
7. **plugins-home gallery `MediaSurface` card kept** (a preview-card naming-collision, not the generation type).
8. **Generic image/video/audio analytics enums restored** (file-type/PNG-export/share — not media generation).
9. **Report chip icon = `file`**; report is a flowing long-form document, not a deck clone.
10. **Connectors-MCP split, not delete** — connector tools verified still mount.
11. **Orbit removed entirely** — the biggest decision. Live-Artifacts removal broke Orbit (it ran on `skillId:'live-artifact'` + a "create and register a Live Artifact" mandate); you chose full removal over repurpose/preserve. General **Automations/Routines are preserved**.
12. **Deck fixes:** kept both window+document key listeners (guarded), not dropping one.
13. **Commits per verified sub-task**, PR/WS-prefixed, **no co-author trailers**, not pushed.

---

## 5. Commits (53 on `v0.2.0`)

Grouped by intended PR (mapping to the 4-PR plan + the added Orbit removal):

- **PR1 (deck fixes):** `8a5b0377`, `759acc7c`, `661ee7f4`, `9a96da0a`.
- **PR2 (media teardown, WS-C+WS-D):** `cedb4981` … `060f92d5`, `06ad9767` (merge of the G1 worktree) — ~30 commits covering chips, daemon engine, BYOK, CLI, prompts, contracts type-purge, content/registry, settings, tests.
- **PR3 (Live Artifacts + Orbit, WS-E+WS-G):** `9d7d752e` (connectors MCP split), `c98c5a15`, `9e1cd557`, `2b646cfb` (FileViewer surgical removal), `ae2dd00b`, `0b124ad4`, `b40078b2`, `efd2d82c` (Orbit removal), `edd8e66b` (e2e cleanup).
- **PR4 (Report, WS-F):** folded into the `2b646cfb` WIP + subsequent reconciliation (chip + `example-report` seed + intent swap).

(Full list: `git log --oneline main..v0.2.0`.)

---

## 6. Verification

- **`pnpm typecheck`** (whole repo): **GREEN** (exit 0) — the media type-purge and the Orbit-removal cascade both fully reconciled.
- **`pnpm guard`**: **GREEN**.
- **Web tests** (`@open-design/web`): **310 files, 3036 pass, 0 fail**.
- **Contracts tests**: **21 files, 159 pass, 0 fail**.
- **Daemon tests**: 3839 pass; the failures split into (a) **two removal-caused regressions — found & FIXED**, and (b) a **pre-existing env/AMR-stripped set** unrelated to this work.
  - **Fixed regression 1 — `routine-schedule-claims` (×6):** the Orbit-daemon removal deleted `formatLocalProjectTimestamp`, which lived in the removed `orbit.ts` but was **also used by the kept routines/automations path**. Because `server.ts` is `@ts-nocheck`, typecheck didn't catch the now-undefined reference; only the daemon tests did. Re-homed the helper as a local function in `server.ts` → routines pass 10/10.
  - **Fixed regression 2 — `plugins-atom-registry` (×1):** an obsolete assertion still expecting the removed `media-image` atom; updated → passes 12/12.
  - **Pre-existing/environmental (unchanged by this work):** `vela`/AMR ×10 (this fork stripped AMR), plus agent-spawn-timeout / filesystem suites (`run-resume-on-failure`, `memory-connectors`, `plugins-build-test`, `plugins-figma-extract`, `server-paths`, `mcp-*`) — the same suites G1 and G3 flagged; the failing set varies run-to-run.
- **Deck red specs:** RED→GREEN confirmed for both WS-A and WS-B.

---

## 7. Adjacent / pending

- **Daemon test triage — complete:** both removal-caused regressions were found and fixed (see §6); the remaining daemon failures are the pre-existing env/AMR-stripped set.
- **Final alignment gate — running:** an independent Opus alignment reviewer is reviewing the full `v0.2.0` against the plan + decisions and running the gates; its `perfect alignment, without caveats` verdict is what declares all three groups DONE.
- **Orbit removal was an unplanned consequence** of the Live-Artifacts excision; scheduled-digest functionality is gone (general automations remain).
- **`pnpm-lock.yaml` changed** (jsdom desktop devDeps added by the deck work) → nominally needs a Nix `pnpm-deps` hash refresh at PR time.
- **Not pushed; no PRs opened.** Commits are grouped to map onto the 4-PR plan (+ Orbit).
